module.exports.arrayCounter = function(array) {
  return `Количество элементов в массиве ${array.length}`;
}

module.exports.multiply = function(x,y) {
  return `${x} умножить на ${y} равняется ${x * y}`;
}

module.exports.plus = function(x,y) {
  return `${x} плюс ${y} равняется ${x + y}`;
}
